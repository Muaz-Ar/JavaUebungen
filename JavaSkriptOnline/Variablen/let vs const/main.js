"use strict";

  let meine_variable = "JavaSkript"; 
  console.log(meine_variable); 

  meine_variable = 42; 
  console.log(meine_variable); 

  // bei variablen die sich nicht ändern dürfen Konstanden 

  const sekunden_in_Stunden = 3600;
  console.log(sekunden_in_Stunden); 
  
  // man kann Konstante == const nicht verändern 
  sekunden_in_Stunden = 60; 

  // es ist nicht möglich eine konstante nur zu deklarieren wie let 
  const pi; 
