"use strict";

// mit promt erzeugt man ein popup Fenster 
// ("1";"2") im abschnitt 1 was abgefragt wird nach dem komm was als bsp sein kann 

// jetzt wird die eingabe in die promt der Variable name zugeordnet 

let vor = "Vor dem promt..." 
consollog(vor)

let name = prompt("Wie ist  dein Name", "Peter Parker"); 
console.log(name); 

let nach = "nach dem promt"
console.log(nach); 

// promts halten das programm an vor wir direkt ausgegeben 
// nach wird erst ausgegeben nachdem das promt ausgegeben wird 